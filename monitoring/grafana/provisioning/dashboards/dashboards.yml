apiVersion: 1

providers:
  - name: 'OpenMail Dashboards'
    orgId: 1
    folder: 'OpenMail'
    folderUid: 'openmail'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
